#Primeiro para preparar o arquivo para commit utilizo
git add <nome do arquivo>

#feito isso é sempre interessante verificar o status dos meus arquivos em reparação
git status

#Para comitar os arquivos que estavam em ppreparação utilizo o comando
git commit 'parte oppcional' -m "mmensagem sobre a alteração"

#então eu dou um push para o github
git push origin master (origin é o repossitório e o master é o branch)


#e para copiar/editar um arquivo do git eu dou um pull
git pull 
